#pragma once
#include "../math/vector.h"
#include "../math/complex.h"

const math::Vector I = {math::Complex(0, -1), math::Complex(1,0), math::Complex(0,0)};
const math::Vector J = {math::Complex(0, 1), math::Complex(1,0), math::Complex(0,0)};